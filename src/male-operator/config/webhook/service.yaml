apiVersion: v1
kind: Service
metadata:
  name: male-operator-webhook-service
  namespace: male-operator-system
spec:
  ports:
  - port: 443
    targetPort: 9443
  selector:
    control-plane: controller-manager

